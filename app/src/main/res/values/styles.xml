<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Добавьте эти стили в ваш файл -->

    <style name="CircleCheckBoxWithTick" parent="Widget.AppCompat.CompoundButton.CheckBox">
        <item name="android:button">@drawable/custom_checkbox</item>
        <item name="android:minWidth">32dp</item>
        <item name="android:minHeight">32dp</item>
        <item name="android:background">@android:color/transparent</item>
    </style>
    <style name="MyCardView" parent="@style/Widget.MaterialComponents.CardView">
        <item name="cardBackgroundColor">#151618</item>
        <item name="cardCornerRadius">12dp</item>
    </style>
    <!-- styles.xml -->


    <style name="RadioButtonStyle" parent="Widget.AppCompat.CompoundButton.RadioButton">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:buttonTint">@android:color/white</item>
        <item name="android:textSize">16sp</item>
    </style>
    <style name="CustomTimePickerDialog" parent="Theme.AppCompat.Dialog">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
    </style>

    <style name="CustomTimePickerStyle" parent="Theme.MaterialComponents.DayNight">
        <!-- Цифры -->
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">16sp</item>

        <!-- Фон селектора -->
        <item name="colorControlActivated">#FFFFFF</item>
        <item name="colorControlNormal">#555555</item>

        <!-- Для API 21+ -->
        <item name="android:numbersTextColor">#FFFFFF</item>
        <item name="android:numbersBackgroundColor">@android:color/transparent</item>
    </style>

    <style name="CustomAlertDialogTheme" parent="Theme.MaterialComponents.DayNight.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@style/DialogAnimation</item>
    </style>

    <style name="DialogAnimation">
        <item name="android:windowEnterAnimation">@android:anim/fade_in</item>
        <item name="android:windowExitAnimation">@android:anim/fade_out</item>
    </style>
    <!-- Цвета для текста -->
    <color name="text_primary">#FFFFFF</color>
    <color name="text_secondary">#CCCCCC</color>
    <color name="text_tertiary">#999999</color>

    <!-- Стиль карточки -->
    <style name="SleepCardStyle" parent="Widget.MaterialComponents.CardView">
        <item name="cardBackgroundColor">#151618</item>
        <item name="cardCornerRadius">12dp</item>
        <item name="cardElevation">2dp</item>
    </style>
</resources>